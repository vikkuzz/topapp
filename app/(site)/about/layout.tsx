import React from "react";

const AboutLayout = ({ children }: { children: React.ReactNode }) => {
  return <div style={{ border: "1px solid cyan" }}>{children}</div>;
};

export default AboutLayout;
